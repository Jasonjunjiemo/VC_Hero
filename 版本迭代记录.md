# 版本迭代记录

本文件为 append only，仅在开发者明确提出新增功能或 bug 修改意见时追加记录。

---

## 2026-08-30 · 初始版本

**开发者要求**：按照 project_requirements.txt 完成 VC_Hero 项目 —— 一个模拟早期 VC Deal Sourcing 面试的 AI 虚拟面试官网站，含账号系统、简历上传（PDF≤10MB、≤5 份、抽取纯文本）、会话管理（≤10 个、可选简历/问题档位/任务数/时长）、两阶段面试（简历问答 → 场景 task）、结束后 A-E 评级与反馈；接入 Kimi 按量计费 API，全局限速（token/h、response/h、每小时 ≤2 元）+ 多用户负载均衡，面试官回复须自我迭代恰好 2 轮后才发出；技术栈 Flask + 原生 HTML/CSS/JS，本地文件系统存储，全程 git 提交，完成后部署到 Ubuntu 服务器。

**实现内容**：
- 后端 Flask（vc_hero 包）：storage（文件系统 JSON 存储）、pdfutil（pypdf 抽文本，抽取失败即上传失败，pdf+txt 均落盘）、ratelimit（全局滑动窗口限速：token/h、response/h、成本上限，Condition 变量 FIFO 公平排队实现负载均衡）、kimi（Kimi API 客户端，草稿 → 恰好 2 轮自我迭代 → 才正式记录发送；429 退避重试）、interview（会话状态机：cv 阶段问满 cv_total 题后进入 task 阶段，每任务 3 回合，耗尽/到时/手动结束触发评分）、routes（REST API）。
- 新增 prompts（均以 md 存放于 prompts/）：Interview_Orchestrator.md（总控，一问一答、不泄题不教学）、Reply_Self_Review.md（回复自检迭代标准）、Scoring_Output_Format.md（评分 JSON 输出格式）、Task_Scenarios.md（16 个可选场景库，建会话时随机抽取）。
- 前端单页应用：登录/注册、个人信息、简历管理、会话管理（新建/重命名/删除）、面试聊天页（倒计时、一问一答、结束评分）、结果面板（评级 + 五维分数条 + 500-1000 字反馈）。
