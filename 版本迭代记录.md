# 版本迭代记录

本文件为 append only，仅在开发者明确提出新增功能或 bug 修改意见时追加记录。

---

## 2026-08-30 · 初始版本

**开发者要求**：按照 project_requirements.txt 完成 VC_Hero 项目 —— 一个模拟早期 VC Deal Sourcing 面试的 AI 虚拟面试官网站，含账号系统、简历上传（PDF≤10MB、≤5 份、抽取纯文本）、会话管理（≤10 个、可选简历/问题档位/任务数/时长）、两阶段面试（简历问答 → 场景 task）、结束后 A-E 评级与反馈；接入 Kimi 按量计费 API，全局限速（token/h、response/h、每小时 ≤2 元）+ 多用户负载均衡，面试官回复须自我迭代恰好 2 轮后才发出；技术栈 Flask + 原生 HTML/CSS/JS，本地文件系统存储，全程 git 提交，完成后部署到 Ubuntu 服务器。

**实现内容**：
- 后端 Flask（vc_hero 包）：storage（文件系统 JSON 存储）、pdfutil（pypdf 抽文本，抽取失败即上传失败，pdf+txt 均落盘）、ratelimit（全局滑动窗口限速：token/h、response/h、成本上限，Condition 变量 FIFO 公平排队实现负载均衡）、kimi（Kimi API 客户端，草稿 → 恰好 2 轮自我迭代 → 才正式记录发送；429 退避重试）、interview（会话状态机：cv 阶段问满 cv_total 题后进入 task 阶段，每任务 3 回合，耗尽/到时/手动结束触发评分）、routes（REST API）。
- 新增 prompts（均以 md 存放于 prompts/）：Interview_Orchestrator.md（总控，一问一答、不泄题不教学）、Reply_Self_Review.md（回复自检迭代标准）、Scoring_Output_Format.md（评分 JSON 输出格式）、Task_Scenarios.md（16 个可选场景库，建会话时随机抽取）。
- 前端单页应用：登录/注册、个人信息、简历管理、会话管理（新建/重命名/删除）、面试聊天页（倒计时、一问一答、结束评分）、结果面板（评级 + 五维分数条 + 500-1000 字反馈）。

---

## 2026-08-30 · 界面与交互修复（7 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 网站主页直接就是个人信息填写，不妥当。主页应该是基本的网站介绍+一些图，然后点击右上角个人头像或用户名才进入个人信息设置。
> 2. 部分按钮内的文字变成两行了，包括（简历的）上传按钮。
> 3. localhost测试新建会话完全失败，首先对话界面完全没有加载出来（期待是立刻有对话界面），再次尝试进入的时候确实能看到会话第一条但是输入框没有显示出来，并且底部文字显示要等待一两分钟，接着干脆直接退回到主页，并弹出窗口显示Cannot set properties of null (setting 'innerHTML')。
> 4. "上传后直接抽取纯文本"为项目要求，写入网站不妥。
> 5. 创建会话时，"问题数量"的三个档位不要写具体的问题个数，也不要写死在AI系统里。
> 6. 创建会话时，最大时长一旦设置到具体数字后就无法重置回"无限制"。
> 7. 进入会话的时候请弹出新浏览器tab，不要直接跳转。

**逐项修复说明：**

1. **主页重构**：主页改为网站介绍页（hero 区标题/介绍 + hero 插图 + 三张功能卡片图），会话管理保留在介绍页下方；个人信息与简历管理移入独立设置页，通过右上角用户名/头像入口进入（`renderSettings`）。新增 `static/img/` 下 4 个 SVG 插图（hero.svg、feature_cv.svg、feature_task.svg、feature_score.svg）。
2. **按钮换行**：CSS 全局 `button { white-space: nowrap; flex: none; }`，简历表单中上传按钮设为 `flex:none`，文件框挤压不再导致文字折行。
3. **新建会话失败（核心 bug）**：根因是异步请求未完成时页面 DOM 已被替换（用户返回主页/重复点击），完成回调里 `document.querySelector('#chat-scroll')` 返回 null 再赋值 `innerHTML` 抛出 TypeError，被 catch 弹出。修复：①所有渲染函数加 null 防护；②`sendAction` 完成/失败回调先校验仍处于同一会话页再渲染，否则直接丢弃；③`busy` 状态在进入会话页时重置，输入框不再被"考官思考中"永久顶掉；④服务端 `start_session` 首个问题生成失败时状态回滚为 created，可重试；⑤创建会话后新标签页立即打开会话界面（见第 7 条）。错误提示改为输入框上方的内联错误条，不再用 alert。
4. **文案删除**：简历上传处删除"上传后自动抽取纯文本"提示，仅保留"仅支持 PDF，单份不超过 10MB"。
5. **问题档位去数字化**：前端三档只显示"低/中/高"，不再显示"3 题/5 题/8 题"；发给 AI 的阶段指令只描述"本阶段提问数量较少/适中/较多"，不再包含具体题目数与进度计数（题目数仍由系统内部状态机控制阶段切换，不进入提示词）。
6. **时长可重置**：创建会话弹层的时长改为"不限时/限时"下拉开关，选"限时"才显示分钟输入框，选回"不限时"即清除限制，不会被数字卡死。
7. **新标签页打开会话**：新增 `/session/<id>` 路由（返回单页应用入口），前端按 URL 自动渲染对应会话；列表中"开始面试/继续面试/查看结果"及"创建并打开"均通过 `window.open(..., '_blank')` 在新标签页打开，不再原地跳转；聊天页"返回"使用 `location.href='/'`。

---

## 2026-08-30 · 交互体验与视觉重做（7 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 个人信息页除了点左上角logo退出没有别的方法退出，非常不合理
> 2. 创建面试后进入的页面里面的"开始面试"按钮以及文字表述应该放页面中间显眼位置，并且上面要有诸如"不限时"等等信息。类似网页端考试开始页面。
> 3. 我自己手测，chrome打开localhost:5000，点击"开始面试"后，只能看到一个空白页面（而我要立刻能看到对话界面），然后下面一行"考官正在思考（回复经过多轮自检，可能需要一两分钟）…"。然后等了很久才有反应。接着我填写我的信息，点击发送按钮，结果编辑框直接消失了，并且又出现了"考官正在思考（回复经过多轮自检，可能需要一两分钟）…"，可能是模型回复太慢了，这不行。可以砍推理时长，而且回复里面应该弹出考官回复的新message，写着正在思考或者三个跳动点之类的显示。
> 4. 我希望按enter可以等价发送回复（按下发送键应该在会话中会立刻出现我发的消息），ctrl+enter才是换行
> 5. 用户反馈界面太低端，太同质化，请整个UI重做，做的高大上一下。
> 6. 低中高默认问的问题数量太少了，低可以是12个上下，中可以是20个上下，高可以是40个上下，但具体几个让模型自己决定。
> 7. 最终反馈不要放进一个灰色背景的框里面，很丑。

**逐项修复说明：**

1. **设置页返回**：个人信息设置页新增"‹ 返回主页"按钮，加上原有顶栏 logo，两个出口。
2. **考试开始页**：会话未开始时不再显示底部小按钮，改为页面中央的开始卡片：会话名、"模拟面试"徽标、信息 chips（简历、问题区间、任务数、不限时/限时 X 分钟）、规则说明、大号渐变"开始面试"按钮；点击后按钮变为"考官准备中…"禁用态，对话区立即出现考官思考气泡。
3. **聊天交互与提速**：①后端 Kimi 调用关闭推理（`thinking: disabled` + temperature 0.6，该模型开启推理时单轮 15-18 秒，关闭后 5-12 秒），面试官每轮 3 次调用总延迟从约 1-2 分钟降到 20-40 秒；②开始面试后立刻渲染完整对话界面，考官回复以带三个跳动点的消息气泡形式出现在对话流中（不再只在底部显示一行文字）；③发送回答后自己的消息立即上屏，输入框保留但禁用（不再消失），考官思考气泡紧跟其后；④服务端开始失败状态回滚（上轮已修）。
4. **Enter 发送**：Enter 直接发送；Ctrl/Cmd/Shift+Enter 换行；输入内容在请求期间保留草稿。
5. **UI 整体重做**：全新视觉体系——靛紫渐变主色、深色渐变 hero 主视觉（含品牌徽标、光晕装饰、白底插图卡片）、毛玻璃顶栏、渐变消息气泡与头像、悬浮动效、圆角与阴影系统升级、登录页加品牌图形背景；会话列表、弹层、按钮等全部统一到新体系。
6. **问题数量区间化**：档位目标改为 低约10-14题 / 中约18-22题 / 高约35-45题（config `LEVEL_RANGES`，含安全上限 18/28/55 防止模型忘记收尾）；具体题数由模型自行决定——阶段指令告知区间并要求模型在收尾回复末尾输出 `[[CV_DONE]]` 标记，系统检测到标记即切换阶段（标记对考生不可见，自检迭代 prompt 要求原样保留标记），达到安全上限则强制切换。
7. **反馈样式**：反馈不再使用灰色背景框，改为白底卡片内的文章式排版（左侧 3px 品牌色竖线 + 1.85 行距），评分条上下用细分隔线，评级改为渐变圆环大字母。



---

## 2026-08-30 · 视觉扁平化与聊天交互优化（12 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 主页图片里有一个很丑的黑色线段，而且两个气泡框下面的三角和气泡框之间断开来了
> 2. 整体UI设计还是过于阴影风格、立体风格，不够简约。
> 3. 主页的面试会话部分放在最底部，难以找到
> 4. 注册和登录按钮风格不统一，注册按钮几乎是灰色的
> 5. 个人信息页里面，返回主页的按钮不够显眼，很容易看不到，并且建议页面底部也加一个类似按钮
> 6. 问题数量不需要显示在新建对话浮窗里
> 7. 创建会话后，鼠标移动到开始面试的按钮时，按钮会突然闪一下
> 8. 发送按钮建议和输入框建议放到同一区域内，可以参考图片中的设计
> 9. 面试官思考时，不要通过禁用编辑框的形式来限制用户发送，而是通过点击发送按钮或enter后跳出提示（不要是那种需要点击确认的消息窗口，单纯是一个几秒自动淡出的、挨着发送按钮上方的类似气泡的提示就可以）表示当前不能发送
> 10. 如果当前会话页面已经拉到底部，此时不管是用户发送新信息还是面试官发送新信息，应该持续保持在底部。
> 11. 编辑框、发送按钮应该固定在页面上，不随着页面滚动而滚动。
> 12. 系统对于用户约束不够，容易被用户带偏，应该加强面试官的控局。尽量用简单方法实现。问问题也太过一致，一直追问同一个东西细节。（你允许修改任何prompt文件包括我最开始的三个md，不过要最小化修改）

**逐项修复说明：**

1. **hero.svg 重绘**：删除黑色矩形线段；两个对话气泡的尾巴改为"先画尾巴、矩形后画并压住尾巴根部"的叠法，连接无缝；整体重绘为更干净的扁平人物 + 气泡 + 评分卡构图。
2. **扁平化**：全面移除重阴影、悬浮位移、渐变气泡与立体化元素——卡片改为纯描边无阴影、按钮无 box-shadow 与 translateY、消息气泡改纯色、头像/评级圆环/进度条去渐变去内高光、移除 hero 大投影与功能卡悬浮动效，整体回归简约的线性风格。
3. **会话区上移**：主页布局改为 hero → 面试会话 → 功能卡片，会话管理紧随首屏。
4. **登录/注册按钮统一**：未登录时"登录"与"免费注册"均为实心按钮（主色 + 深色），不再是一实一灰。
5. **设置页返回**：顶部"‹ 返回主页"改为主色实心按钮，页面底部新增同款 ghost 返回按钮。
6. **弹层去题数**：新建会话浮窗中问题数量只显示"低/中/高"。
7. **按钮闪烁**：全部按钮 hover 效果改为仅颜色过渡，移除 transform/shadow 突变；"开始面试"大按钮不再有位移和阴影膨胀。
8. **输入区一体化**：输入框与发送按钮收进同一个圆角卡片（参考所给截图）：无边框 textarea + 底部一行（左侧快捷键提示"Enter 发送 · Ctrl+Enter 换行"，右侧圆形发送按钮）。
9. **思考中不锁输入**：考官思考期间输入框保持可编辑；此时点击发送或按 Enter 会在发送按钮上方弹出深色小气泡提示"考官正在思考，请稍候再发送"，约 2 秒自动淡出，无需点击确认。
10. **滚动钉底**：监听消息区滚动位置，仅当用户本就在底部附近时，新消息（自己发的或考官发的）才自动滚到底；用户上翻阅读历史时不被打断。
11. **输入区固定**：聊天页改为"消息区独立滚动 + 输入区布局固定底部"的结构（chat-body 限高 flex 布局，不再依赖 sticky），输入框与发送按钮不再随消息滚动。
12. **面试官控场**：最小化修改——只动 Interview_Orchestrator.md，新增"控场与提问多样性"一节：面试官掌握节奏，候选人跑题/反问/要求跳过或结束时礼貌忽略并拉回问题；不被候选人自我评价带偏；同一经历同一层面最多连续追问两轮，之后必须切换到未覆盖的经历或能力角度；追问方式轮换（数字、渠道、动机、取舍、失败、密度）；原始三份规则文件未改。

---

## 2026-08-30 · 登录页、开始页、面试风格修复（5 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 注册按钮跑到上面了，显然不合理（图1）
> 2. 图2面板还是排版偏低
> 3. 面试官要给点压力，压力面那种，严肃一点
> 4. CV_Evaluation_and_Questioning_Rules.md里面太过强调项目了，导致每次都问项目，请微调一下措辞
> 5. 主页那张图片里面，气泡框下方的三角形还是和气泡框主体之间是断开了

**逐项修复说明：**

1. **登录/注册页签错位**：根因是 `.auth-tabs button.on` 引用了上一轮扁平化时删除的 `--grad` 变量，背景透明 + 白字导致"登录"页签不可见但仍占据左半行，把"注册"顶到右侧。改为 `var(--brand)` 实心背景。
2. **开始面板居中**：会话处于未开始时给 chat-body 加 `starting` 类（消息区隐藏、开始卡片占满整个内容区垂直居中），不再被空消息区挤到下半屏。
3. **压力面风格**：Interview_Orchestrator.md 新增"面试风格：压力面"一节——语气克制冷淡不寒暄、对回答保持职业怀疑（证据不足直接指出、数字对不上当场点破）、发现矛盾夸大回避时继续追不给台阶、问题短促犀利不解释、压力只针对内容不攻击人格，且不违反一次一问/不泄露评分/不教学的基本规则。
4. **CV 规则去项目化**：在 CV_Evaluation_and_Questioning_Rules.md 开头说明段后新增一段：原则适用于各类经历（科研、实习、社团、竞赛、兼职、志愿、课程成果及任何主动做过的事），不要只盯着"项目"提问，应在不同类型经历间切换。其余原文未动。
5. **SVG 气泡尾巴**：放弃"矩形+独立三角"拼法，将气泡与尾巴合并为单个 path（圆角矩形轮廓与三角形尾巴一体成形），描边连续，白气泡带描边、蓝气泡纯色，结构上不再可能断开。

---

## 2026-08-30 · 内联改名、Hero 缩小 + AI 训练官新功能

**开发者明确要求（原文照录）：**

> 修复：
> 1. 重命名居然是弹出弹窗，这样看起来太拉跨，应该是直接在会话名显示的位置上直接修改。
> 2. 图片中这个面板稍微有点大，可以缩小20%
>
> 新增功能：
> 加入（Training_Guide_Rules.md）功能，可以有两个tab，可以切换两个part，第一个part就是现在的AI面试官，第二个part是AI 训练官
> 副标题：Deal Sourcing 工作场景训练
> 标语：在真正面试之前，先练成行业老炮
> 按钮：立即开始模拟训练
>
> 点击可以在tab之间切换，不要redirect，直接做丝滑的过渡切换，整个页面上的几乎所有内容都要切到另一个tab上。面试会话和学习会话分开面板管理。
>
> 这个AI训练官复用之前写的面试的时候用的会话管理代码（比如UI、llm api等），但是提示词和会话流程控制换一套。
>
> 创建学习会话的时候，提供两种选择，一种是直接空白开始会话（等于没有任何记忆），一种是导入预设上下文，提示词可以是用户自己上传（可以允许复制粘贴、上传pdf、txt、md、docx），也可以是从某个面试会话那里导入（导入完整的会话，包括对话以及最终评分和反馈等）

**修复说明：**
1. **内联重命名**：会话列表的"重命名"不再用 prompt 弹窗，点击后直接把会话名替换为输入框（Enter 保存、Esc 取消、失焦保存），保存后原地刷新列表。
2. **Hero 缩小约 20%**：hero 内边距 48→36、标题 32→26px、插图最大宽度 400→330、正文字号与光晕同步缩减。

**新增功能实现（AI 训练官）：**
- **双 tab 主页**：顶部"AI 面试官 / AI 训练官"分段切换器，切换不跳转、带淡入上移动画，hero 文案、功能卡片、会话面板整套内容随 tab 切换（训练官 tab：副标题"Deal Sourcing 工作场景训练"、标语"在真正面试之前，先练成行业老炮"、按钮"立即开始模拟训练"）。
- **独立会话面板**：会话增加 kind 字段（interview/training），面试会话与学习会话分开列表、各自独立计数上限（各 10 个），互不混杂。
- **复用与分流**：训练会话完全复用现有 UI（聊天页、composer、乐观回显、思考气泡、toast、滚动钉底）与 LLM 客户端（限速、2 轮自检）；服务端 `/start /answer /finish` 按 kind 分发到训练引擎（新模块 `vc_hero/training.py`）。
- **新流程**：自由回合制——训练官布置场景 → 学员作答 → 训练官点评对错、讲解正确做法、推进下一步（无题数/无阶段计数）；随时"结束训练"生成**训练总结**（进步/主要问题/下一步场景建议，无评级打分），展示在结果页（无评级圆环）。
- **新提示词**（均存 prompts/）：Training_Guide_Rules.md（训练官角色、场景库、点评+教学+推进规则、上下文使用方式）、Training_Reply_Self_Review.md（训练版自检：保留点评与教学结构，区别于面试版的"无教学"约束）；原 Reply_Self_Review.md 措辞泛化为考官/训练官通用。
- **创建学习会话的两种起点**：① 空白开始（无记忆）；② 导入预设上下文，三种来源——复制粘贴文本、上传文件（PDF/TXT/MD/DOCX，服务端抽取纯文本，docx 用 zipfile 解 XML 无额外依赖；新端点 POST /api/context-files）、从面试会话导入（服务端把完整对话+最终评级+反馈拼成上下文文本，context_label 显示来源）。
- 实测：导入"上轮面试被指出不会判断渠道优先级"的上下文后，训练官开场即针对性设计训练；学员作答后回复结构为"这三个动作全错→讲解正确做法与原因→追问下一步"；结束总结包含进步、引用原话的问题清单、下一步场景建议。

---

## 2026-08-30 · Tab 控件样式、滑动过渡、未登录顶栏、行动结果模拟（4 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 你上方切换tab的两个东西长得更像按钮而不像tab切换，而且里面不应该有多余文字（比如模拟面试 A-E评级这类的）
> 2. 切换的时候请左右滑动内容来过渡，不是突然闪一下
> 3. 整个网站主页页面右上角应该也得有登录注册的选项
> 4. 面试过程中如果出现task，面试官可以提供一些信息选项，比如候选人attempt了一个XX action的回答，面试官可以说假如你尝试该action之后出现了XX情况、XX结果，你下一步会怎么做。并不是每次都要这么问但是可以作为一个可选项写进md。

**逐项修复说明：**

1. **Tab 控件重做**：改为 iOS 分段控制器样式——灰色圆角轨道内嵌白色药丸指示器（激活项），只显示"AI 面试官 / AI 训练官"两个词，删除副标题等所有多余文字，不再有按钮外观。
2. **左右滑动过渡**：切换时按方向滑入——切到训练官（向右）内容从右侧滑入，切回面试官（向左）从左侧滑入（`slide-l`/`slide-r` 位移动画），替代原来的闪现淡入。
3. **未登录顶栏**：主页未登录时也显示顶栏，右上角"登录 / 注册"两个入口，分别直达对应页签。
4. **行动结果模拟**：在 Deal_Sourcing_Knowhow_Task_and_Questioning.md 末尾新增第三十四节——候选人给出具体行动后，面试官可选地假设该行动已执行并给出具体结果（顺利/受挫/意外/冷淡/数据对不上），追问"你下一步怎么做"；明确这是可选项（一个任务用一到两次）、不在方案明显错误时替代直接指出问题，并附两个示例。

---

## 2026-08-30 · 整页滑动切换、训练 tab 配色、上传交互与贴底修复（10 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. tab切换时的左右滑动应该是整页都划走，不是象征性地滑一下，而且切换的时候你会出现闪烁问题
> 2. 两个tab的页面都共用同一个图1中的面板，不太好，AI训练官应该重新设计和配色，但保持这个面板的一些统一性，譬如面板里那个白色子图像的位置
> 3. 创建会话的时候如果弹窗提示需要有至少一份简历，应该在弹窗内加入一点就可以切到个人页面的按钮
> 4. 选择完（CV）文件就应该直接上传，不需要额外的上传按钮。
> 5. 选择文件按钮和周围不统一，很丑，建议直接变成一个加号，点一下就可以选文件，并且UI要统一。
> 6. 点击退出以退登的时候，要弹出提示框确认是否确实要退登。
> 7. "从面试会话导入"这里不要直接设置为不可选（当面试会话为空的时候），而是出现一个空列表并且显示暂无面试会话
> 8. 新建学习会话的时候的上传文件按钮也是太原始，UI丑
> 9. 如图2，输入框居然跑到屏幕中间，而不是贴底。
> 10. 任何情况下，点击新建会话（不管是面试还是学习）都应该立刻出现会话界面所有UI元素，但是居然是先空白，然后要等一会才有。

**逐项修复说明：**

1. **整页滑动 + 消除闪烁**：切换逻辑重构——不再整体重建页面，顶栏与切换器保持不动，仅 `#tab-host` 内容区做动画：旧内容整页滑出屏幕（translateX ±100%），新内容从对侧整页滑入；数据在滑出动画期间预取，避免闪烁。滑入/滑出动画类 `slide-in-l/r`、`slide-out-l/r`。
2. **训练 tab 独立配色**：训练官 hero 改为墨绿→深青→翠绿的独立渐变（`.hero.training`）， eyebrow/正文/光晕配色同步调整；布局结构、白色子图位置与尺寸保持与面试 tab 一致。
3. **无简历引导弹窗**：创建面试会话无简历时，不再 alert，改为弹窗提示"还没有简历" + "去上传简历"按钮，点击直达个人信息设置页。
4. **选中即传**：简历选择文件后立即上传，移除"上传"按钮，文件名旁显示上传状态。
5. **加号文件按钮**：原生文件选择框隐藏，替换为统一的虚线圆角加号按钮（`.plus-btn`，hover 变品牌色），设置页与训练弹层同款。
6. **退出确认**：点击退出先弹确认框"确定要退出登录吗？"。
7. **导入选项不置灰**："从面试会话导入"单选始终可选；无面试会话时下拉显示"暂无面试会话（先去完成一场模拟面试）"，提交时校验拦截。
8. **训练弹层文件按钮**：同样换成加号按钮 + 文件名/解析状态显示。
9. **输入框贴底**：根因是上一轮给 `#chat-foot` 加了 `flex:1`，与消息区平分高度把输入区顶到屏幕中间。改为默认 `flex:none` 贴底，仅开始卡片状态（`starting`）占满剩余高度居中。
10. **会话页即时渲染**：index.html 加入加载占位（不再白屏）；聊天页改为先渲染顶栏骨架再异步填充会话内容，新建/打开会话时所有 UI 元素立即就位。

---

## 2026-08-30 · 首屏服务端渲染、滑动方向、悬浮顶栏等（8 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 个人信息设置这几个字建议居中
> 2. 你切换tab的左右移动不是让你按一下就一左一右动，而是按一下，只往一个方向移动，比如按左边的tab就移到左边，按第二个tab就移到右边
> 3. AI训练官那个绿色面板换成淡蓝色（也就是原来AI面试官那里可以看成是深蓝色或者蓝色，不要动面试官那里，只动训练官），然后小人的那张图完全给AI训练官那里重新设计。
> 4. 点击开始面试/开始训练的时候，会话页面内容（比如第一条消息和编辑框等）还是没有第一时间瞬间跳出来，这是很差的情况。之前说过好几次了，这次务必解决了。
> 5. 刚进入学习会话的时候，没有那个三个点的加载中的东西。过一会又突然有面试官消息。
> 6. 会话滚动条太丑了，应该放页面最右侧
> 7. 还有导航栏这条白色的区域会挤压对话可见区域，可以去掉，但是导航栏上的这些按钮元素等还是要保留
> 8. AI训练官那边太苛刻了，你调整一下提示词，碰到问题当然可以指出，但有亮点也要鼓励，并提出对方可以去了解和学习的领域或者是可以怎么去做

**逐项修复说明：**

1. **设置页标题居中**："个人信息设置"改为在页头容器中水平居中，返回按钮绝对定位在左侧，不再把标题挤偏。
2. **单向滑动**：修正切换逻辑——点右边的 tab（训练官）时旧内容向左滑出、新内容从右侧滑入；点左边的 tab（面试官）反之。每次点击只朝一个方向运动。
3. **训练官淡蓝面板 + 新插图**：hero 配色改为淡蓝渐变（#274b7e → #4a7fd4 → #7fafe8），面试官深蓝面板不动；训练 tab 使用全新绘制的 `hero_training.svg`（师徒构图：深蓝训练官 + 浅蓝学员、教学气泡、训练进度卡），白色子图位置与尺寸保持一致。
4. **首条消息瞬间出现（核心）**：改为服务端渲染首屏——`/start` 接口同步生成第一条消息，HTTP 响应直接携带完整会话；`/session/<id>` 对已开始的会话直接渲染 `templates/session.html`（顶栏、消息、输入框全部在初始 HTML 里），前端仅做"水合"接管交互（发送/结束/滚动），不再二次渲染、不再有空窗期。实测刷新页面时第一条消息和输入框直接在 HTML 中。
5. **思考气泡常驻**：服务端渲染的会话页进入即是完整对话；点击开始后响应携带首条消息+思考气泡流程（乐观回显 + 三点气泡），不存在"空白→突然冒出消息"。
6. **细滚动条**：消息区/弹层统一 6px 细滚动条（`scrollbar-width: thin` + webkit 定制），紧贴容器右缘，透明轨道。
7. **悬浮顶栏**：顶栏改为 fixed 浮岛（居中圆角毛玻璃条，不占文档流），对话内容用整屏高度，顶栏不再挤压可见区域；返回/标题/计时/结束按钮全部保留。
8. **训练官口吻平衡**：Training_Guide_Rules.md 调整——点评要平衡：指出问题用词直接，但有亮点必须明确肯定并说明原因；每次点评结尾给出可执行的改进方向（推荐了解的领域/渠道/案例或直接可采取的动作），不再只是挑错；"严格的实战导师"改为"既严格也愿意带人的实战导师"。

---

## 2026-08-30 · 轨道式滑动与全宽透明顶栏（2 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 你现在切换tab的时候还是一瞬间一个页往左然后一个页往右，而不是整体往左（右）
> 2. 你现在登录注册按钮都快跑到中间了，而不是在最右边，而且导航栏白底依然还在，甚至直接把第一条消息给挡住了。左上的logo也跑到快中间的位置了。

**逐项修复说明：**

1. **轨道式整体滑动**：改为 carousel 轨道——切换时把当前面板和目标面板并排渲染成 200% 宽的双面板轨道，点右边的 tab 轨道整体左移（当前页从左侧离场、新页从右侧进场，同一方向连贯平移），点左边的 tab 轨道整体右移；动画结束后再收敛为单面板。不再是"一个往左一个往右"的替换式动画。
2. **全宽透明顶栏**：顶栏改为全宽、完全透明、无边框无白底——logo 贴视口左缘，用户/退出（未登录时为登录/注册）贴视口右缘，不再居中成岛；对话区与主页内容增加顶部留白（64px/76px），第一条消息不再被遮挡。

---

## 2026-08-30 · 顶栏入流、滚动条贴边与静态资源缓存修复（2 项）

**开发者明确要求修复以下问题（原文照录）：**

> 1. 导航栏依然会挡住会话，并且主页的切换tab的控件也被挤下去了，你完全没有测试和详细审计
> 2. 会话滚动条依然不是在页面最右侧，这个你也是没有测试和详细审计

**逐项修复说明：**

1. **顶栏改为文档流内布局**：放弃 fixed 悬浮方案——顶栏回到文档流（position: static）、全宽、透明无底无边框，按钮自然分布左右；对话区从顶栏下方开始（实测：顶栏底 56px、会话区顶 56px、首条消息 66px，结构上不可能再遮挡）；主页内容恢复原有顶部间距（实测切换控件位于顶栏下方 84px，即正常的 56+28 间距，不再被额外下挤）。
2. **滚动条贴视口最右缘**：消息滚动容器改为全宽（不再受 880px 内容列限制），内部新增 880px 居中的 `.chat-inner` 内容列承载消息——滚动条因此位于视口最右侧（实测 right gap = 0），消息列与输入框保持居中且对齐。服务端模板与 SPA 双路径同步改造，发送/思考气泡/滚动钉底全链路复测通过。
3. **附带发现的根因（重要）**：Flask 默认静态资源缓存 12 小时，浏览器一直使用旧版 app.js/style.css——此前多轮"改了但你看不到效果"的投诉（"依然挡住""依然在中间"等）很大程度上是缓存所致。已设置 `SEND_FILE_MAX_AGE_DEFAULT = 0`，静态资源每次校验、永不久存。

---

## 2026-08-30 · 静态资源版本号防缓存（同一问题的根治）

**背景**：开发者再次反馈"导航栏依然挡住会话、tab 控件依然被挤下"。在 1440×900 桌面尺寸下做了逐元素像素审计：顶栏 position=static、顶边 0、高 56、透明底；会话区顶 56、首条消息未被遮挡；切换控件位于顶栏下方 84px（56+28 正常间距）、水平居中——即服务端代码已无问题，用户端仍看到旧布局的唯一解释是 **Chrome 磁盘缓存**：修复前下发的 `Cache-Control: max-age=43200`（Flask 默认 12 小时）使浏览器在过期前根本不向服务器校验，后续的服务端改动用户完全收不到。

**修复**：
1. `SEND_FILE_MAX_AGE_DEFAULT = 0`（上轮已加）：资源每次访问都回源校验。
2. **版本号 cache busting（本轮新增）**：index.html 移入 templates，CSS/JS 引用改为 `/static/style.css?v=<app.js 与 style.css 的 mtime 较大值>`，服务端启动时计算——文件一变 URL 就变，浏览器视为全新资源，旧缓存彻底失效，无需用户手动强刷；`/` 与 `/session/<id>` 两个入口均通过模板注入版本号。

---

## 2026-08-30 · 消息区上缘渐隐（滚动裁剪观感修复）

**开发者反馈（原文照录）**：截图显示滚动中的会话顶部一条消息气泡被生硬切断（"上方不还是明显遮挡吗"）。

**说明与修复**：顶栏已改为文档流内布局（position: static），物理上不可能覆盖会话区（1440×900 审计：栏底 56px、内容区从 56px 开始）；截图所示"遮挡"实为消息在滚动区上边缘的滚动裁剪，此前无任何过渡，观感像被挡住。修复：1) 内容列顶部留白 4→14px；2) 滚离顶部超过 8px 时给滚动区加 32px 渐隐遮罩（CSS mask，ChatGPT 同款），消息向上滚动时自然淡出而非硬切断；回到顶部时遮罩自动移除，第一条消息完整清晰。SPA 与服务端渲染（boot 水合）两条路径均已接入滚动监听。

---

## 2026-08-30 · HTML 响应禁用缓存（缓存问题的最终一环）

**背景**：开发者在已修复的布局上仍看到旧表现（消息在视口顶部被切断、顶栏消失）。1440×900 逐像素审计确认服务端布局无文档级滚动（docScrollH=900=innerH，顶栏 top=0、会话区 56~900），问题只能来自浏览器缓存。排查发现最后一环：render_template 的 HTML 响应没有 Cache-Control 头，Chrome 按 Last-Modified 启发式缓存了**旧版 HTML 页面**，旧 HTML 引用的是无版本号的旧 JS/CSS URL，与磁盘里的旧静态资源互相"复活"，版本号防缓存因此始终未被触发。

**修复**：`after_request` 给所有 text/html 响应加 `Cache-Control: no-cache, no-store, must-revalidate`——HTML 每次必回源，拿到的永远是带最新版本号的页面，进而加载最新静态资源。至此缓存链（HTML → 带 v 的 CSS/JS → max-age=0 校验）全部闭环，任何环境下刷新即最新。

---

## 2026-08-30 · 顶栏定稿：文档流实心栏，移除渐隐遮罩

**背景**：开发者截图显示滚动中的消息发灰、观感像被顶栏压住。反思定位：上一轮加的"滚动区上缘渐隐遮罩"（CSS mask）本身就是问题来源——半透明遮罩让正常滚动的消息看起来发灰、像被遮挡；此前多轮反复则是浏览器缓存（已根治）与验证环境退化共同造成的。

**定稿方案（最保守可靠）**：顶栏回到文档流内（position: static）、全宽、白色实心背景 + 底部分隔线——与 ChatGPT/微信网页版同一模式：栏在结构上占用自己那一行，任何内容物理上不可能出现在它下方，"遮挡"从机制上被根除；对话区占满剩余视口（calc(100dvh - 57px)），滚动条贴视口右缘。同时移除渐隐遮罩及其 JS 逻辑，消息滚动恢复自然硬边界。实测：栏底 56px、会话区自 56px 起、文档高=视口高（无页面级滚动）、遮罩已移除。

---

## 2026-08-30 · 聊天页取消顶栏横条，改为 DeepSeek 式透明浮动控件

**开发者需求（原文核心）**："让整个上面的遮挡都没有……DeepSeek 会话界面顶栏就完全不存在，他是怎么做到的"。

**实现**：聊天页的"顶栏"不再是一条占空间的横栏，改为 `.chat-floatbar`——position:fixed、完全透明、pointer-events:none（栏本身不拦截任何内容）的浮动控件层：左上角"‹ 返回"ghost 按钮、居中会话名（纯文字）、右上计时+"结束面试/训练"。对话区（.chat-body）高度改为 100dvh，**内容从页面最顶端（y=0）开始铺满整个视口**，与 DeepSeek 一致；内容列顶部留 56px 避免首条消息与浮动按钮重叠，滚动条贴视口右缘。主页/设置页保留标准顶栏不受影响。SPA（renderChat）与服务端渲染（session.html）两条路径同步改造。实测：floatbar 透明 fixed 48px、会话区 top=0 高=视口高、滚动条右边距 0。

---

## 2026-08-30 · 主页顶栏取消横条、聊天页标题移至右上（2 项）

**开发者要求（原文照录）**："主页顶栏也要去掉。还有对话页面顶栏中间有字会遮挡，可以移动到右上角和右上角小灰字放一起"。

**修复**：
1. 主页（及设置页、未登录态）的顶栏横条取消，复用聊天页的 `.chat-floatbar` 方案：左上角 logo、右上角用户名/退出（未登录为登录/注册）透明浮动，不占任何文档流空间；页面内容整体上移，hero 紧随 tab 切换器。实测主页无 `.topbar`、floatbar 透明 fixed、logo 左边距 16px。
2. 聊天页浮动控件栏中间不再放会话名——标题（含"训练"徽标）移入右上簇，与计时小灰字、"结束面试/训练"并排（实测右簇顺序：标题 → 计时 → 结束按钮），不再有任何元素悬浮在对话内容中央上方。

---

## 2026-08-30 · 浮动控件实心化（1 项）

**开发者反馈（原文照录）**："顶栏虽然看不到了，但是顶栏这些元素现在变成这种半透明的了而且会叠加在页面内容上面，现在元素本身要做成不透明的"。

**修复**：浮动层本身保持透明，但每个直接子控件（返回按钮、右侧簇容器、logo）改为实心白 pill——白底 + 描边 + 轻投影（`.chat-floatbar > *`），叠在滚动内容上完全不透；ghost 按钮单独提权覆盖（`.chat-floatbar > button.ghost`），用户名按钮保持透明嵌在右侧 pill 内。实测返回按钮与右上簇（标题+计时+结束）背景均为 rgb(255,255,255)。

---

## 2026-08-30 · 修复消息列挤压居中（flex 布局冲突）

**开发者反馈**："新建会话进去是空的"；截图显示消息挤在中央窄列，左右不分。

**根因**：`.chat-scroll` 遗留早期"直接承载消息"时的 `display:flex; flex-direction:column`；后来消息移入子元素 `.chat-inner`（`margin:0 auto`）后，flex 布局中 auto 边距优先于 stretch，导致 chat-inner 收缩为内容宽度、整体挤在页面中央。开始卡片、输入框不受影响（不同分支），但消息区看上去"空的/缩在一起"。

**修复**：`.chat-scroll` 退化为纯滚动容器（display:block），布局完全交给 `.chat-inner`。实测 1440 宽下消息列 880px、考生气泡右对齐（距右 355px 含头像）、考官气泡左侧 345px，左右展开正常。

---

## 2026-08-30 · 修复点击"开始面试/训练"后整页空白（2 项）

**开发者反馈（原文照录）**："新建会话之后进去是空的，连编辑框和发送按钮都没显示，要退出后继续会话进去才有东西"；附截图：点开始按钮后页面只剩浮动按钮、主体全空。

**根因**：点击开始后、服务端响应返回前，`state.current.status` 仍是 `created`，`renderChatFoot` 给 chat-body 挂的 `starting` 类会把消息区 `.chat-scroll` 整个 `display:none`；同一时刻 busy 分支又清空了底部区域——思考气泡其实已渲染但被隐藏，用户看到的就是空白页。等响应到达状态变 active 才恢复，退出重进（服务端渲染）因此"才有东西"。

**修复**：`starting` 判定改为 `status === 'created' && !busy`——开始请求进行中立即切换为对话视图，思考气泡可见。实测点击开始后：starting 类移除、消息区可见、三点思考气泡正常渲染。

---

## 2026-08-30 · 开始阶段显示输入框（1 项）

**开发者反馈（原文照录）**：截图显示思考气泡已出现但"会话消息出来了，输入框没出来"。

**修复**：点击"开始面试/训练"后的等待期，底部不再清空——与进行中状态一致渲染输入区（composerHtml 抽取共用），用户可以在等首条消息时预输入；此时发送会弹出"考官正在思考"提示（原有逻辑）。实测开始等待期：思考气泡可见、输入框可见、可编辑。

---

## 2026-08-30 · 修复切换 tab 时会话面板"闪一下"展开（1 项）

**开发者反馈（原文照录）**："切换tab的时候，面试会话和学习会话那两个面板会突然从不展开变成展开，视觉上感觉就是闪了一下"。

**根因（代码审查确认）**：`tabContentHtml` 构建面板时，会话列表容器 `<div id="session-list">` 是**空的**——列表行由 `renderSessionList()` 在 `bindTabContent()` 里二次注入，而 `switchTab` 中 `bindTabContent()` 只在滑动动画结束（330ms 后）才调用。于是滑动过程中面板卡片只有标题（矮），动画一结束行数据注入、卡片突然撑高，视觉上就是"闪了一下"。

**修复**：会话行改为在面板 HTML 构建时一次性内联渲染（`sessionListInnerHtml`，含空态文案），`bindTabContent` 只负责补事件绑定，面板高度从滑动第一帧起就是最终高度，无二次撑开。顺带加固：`switchTab` 加 try/finally，异常时复位 `switching` 锁，避免切换卡死。

---

## 2026-08-30 · 训练弹层加号偏移修复 + 新建学习会话可勾选简历（2 项）

**开发者要求（原文照录）**："新建学习会话那里应该要可以勾选（或不选）已经上传的简历，这个属于新增功能。另外新建学习会话里面的上传文件的加号按钮的+画歪了，跑到左上了，应该做成和个人信息页那个上传文件的加号一样才对"。

**修复与实现：**
1. **加号偏移根因**：弹层表单样式 `.modal label { display: block }` 特异性高于 `.plus-btn` 的 `inline-flex`，导致弹层内的加号按钮退化为 block、svg 失去居中（实测 offset 1,1）。修复：`.modal label.plus-btn` 提权还原 inline-flex（实测 svg 居中 11,11，与个人信息页一致）。
2. **新增功能：新建学习会话可附带简历**：弹层新增"附带简历（可选）"勾选区（列出已上传简历，多选，可不选；无简历时提示去上传）。后端 `context_resume_ids` 校验归属后，把所选简历抽取文本拼入训练上下文（每份截 8000 字），`context_type` 自动置为 text、`context_label` 显示"简历：文件名"。可与粘贴材料/面试记录导入叠加使用。冒烟测试覆盖（32 项全过）。

---

## 2026-08-30 · 配额上调（开发者要求）

**要求（原文照录）**："配额调到一个小时五块钱然后重新上线，其他的限制也可以酌情调高"。

**调整**（vc_hero/config.py，服务器已重启生效并线上验证）：每小时成本上限 2 元 → 5 元；token/h 150,000 → 400,000；response/h 90 → 200。其余机制（滑动窗口、FIFO 公平队列、预占-实结、保守偏高计价）不变。

---

## 2026-08-30 · 面试官口吻降 mean（开发者要求）

**要求（原文照录）**："AI面试官有点太mean了，稍微改一下提示词，老是怀疑事实。"

**调整**（仅 Interview_Orchestrator.md 压力面一节）：追问的定位从"职业怀疑"改为"核实和理解"——无明显矛盾时先采信回答再追细节；只有前后矛盾、数字对不上、明显夸大或回避时才直接指出；指出问题给解释空间、听完再判断、不连续否定、不把追问变成审问；禁用"这个说法我没办法验证""你说的我不信"等全盘否定式措辞。其余规则不动。

---

## 2026-08-30 · 面试官口吻与评分校准（3 项，开发者要求）

**要求（原文照录）**："追问以核实为目的这个是不对的，是考察能力为目的，不要老是动不动就怀疑，除非用户确实说一些太离谱的经历或展现出的能力明显低于简历"；"他追问的太猛了，问到足够深度后换一个角度问"；"评分太低了，有点严格，调整一下提示词，并且你自己测一下"。

**调整：**
1. **Interview_Orchestrator 压力面节重写**：追问目的明确为"考察能力"而非核实事实——默认采信回答、把回答当素材往下考察（怎么想的、为什么这么做、能否复用）；只有经历明显离谱或展现能力明显低于简历时才质疑，且针对矛盾点不否定人。
2. **控场节加重深度控制**：一个角度挖到足够深度（拿到具体动作/数字/方法/原因）后必须换角度或换经历，每段经历一般 2-4 问即收束，不反复纠缠、不连珠炮追问。
3. **Interview_Scoring_Rules 评分校准**：开头加"以真实候选人合理水平为参照、不拿成熟投资人标准苛求"；第八条改"区分度不等于压分、65-80 是常态区间、没答全不重扣"；第九条"80+ 大方给"；新增第十一条"证据不足默认 65-75 并注明材料有限，不下能力定论，低分只给证据充足且确实薄弱的情况"。

**自测**：相同两轮普通回答（社群冷启动+嘉宾邀请），校准前 D/59.0（55-65），校准后 **C/70.0**（68-72），命中目标默认带；完整面试（含 task 阶段）仍按实际表现走真实评分带，优秀者可到 B/A。

---

## 2026-08-30 · 新增"继续面试/继续训练"链式续聊功能（开发者要求）

**要求（原文照录）**：面试结束后可点击按钮继续会话——① 前一个"面试结果"保留但不进入继续会话的 AI 上下文；② 会话从结果面板下方继续，新消息渲染在面板下方；③ 继续不开启新 task，从前一个 task 结尾继续；④ AI 不知道之前停过，语气延续；⑤ 过几轮后在合理时间点自动生成新结果，可再次继续；⑥ 训练官导入面试结果功能同步适配（含每次结果）。补记：结束后右上角按钮切换为"继续面试/继续训练"（继续后切回"结束"），结果面板底部同时有继续按钮（双按钮）。

**实现**：
- **后端**：会话新增 `continuation_mode`/`since_result`/`result_boundary`/`results_history` 字段；`POST /sessions/<id>/continue`（双引擎通用）——结束后继续：状态回 active、续聊时先生成考官/训练官的下一条消息（结束后消息流停在考生侧，直接答题会触发一问一答校验）；续聊满 `CONTINUATION_AUTO_RESULT_ANSWERS`(6) 轮自动生成新一轮结果，也可随时手动结束；`_score`/`_summary` 生成新结果前把上一份结果快照（含当时消息边界）存入 `results_history`（仅供展示与训练导入，不进 AI 上下文）；继续模式跳过任务/阶段推进逻辑，task_index 钳位，AI 侧完全无感知中断。
- **前端**：消息流与历史结果面板按边界交错渲染（面板留在消息流中、新消息在最新面板下方）；结果面板下方"继续面试/继续训练"大按钮 + 右上角浮动按钮随状态切换（scored→继续，active→结束，双按钮）；可无限链式继续。修复会话 id 路由正则不支持下划线/连字符的潜在 bug（`[^/?]+`）。
- **训练端**：训练引擎同款续聊 + 满 6 轮自动生成新训练总结 + 历史总结快照；UI 与面试端完全统一。
- **训练导入面试记录**：`_interview_import_text` 改为输出完整对话 + 每一次面试结果（第N次评级/反馈，最新一份标注），服务端拼装，训练弹层无改动。
- **测试**：冒烟 40 项全过（双引擎续聊、自动结果、历史快照、再次继续）；无头 Chrome CDP 端到端验证前端：scored 页消息/面板交错顺序 [msg×4, PANEL, msg, PANEL, continue-bar]、双继续按钮、点击继续出现输入框且右上角切回"结束"、回答渲染在最新面板下方、训练端同链路。

---

## 2026-08-30 · 修复：点击继续后上一份面试结果面板消失（bug）

**反馈（原文照录）**："前一个'面试结果'保留，这个你没做到，我点了一下继续，前面面试结果直接没了"。

**根因**：第一份结果存于 `result` 字段（`results_history` 只在生成第二份时才追加快照），而前端渲染当前结果面板的条件是 `status === 'scored'`——点击继续后状态变 active，该面板即被跳过，历史快照又无内容，面板消失。

**修复**：renderMessages 改为把"当前结果 + result_boundary 消息边界"与历史快照合并排序后统一交错渲染，与状态无关（旧会话无 boundary 字段时回退为全部消息之后）。E2E 回归：点击继续后消息流顺序 [4条消息, 历史面板, 考官新问题, 当前结果面板]，面板保留、新消息在其下方。

---

## 2026-08-30 · 继续按钮点击即时反馈（1 项）

**反馈（原文照录）**："点击继续训练或者继续面试的话，点击按钮之后没有即刻反应，看起来像是网站卡死了一样"。

**修复**：continueInterview 在点击瞬间即进入 busy 态——面板下方继续按钮禁用并改名"准备中…"、结果面板下方立即渲染三点思考气泡、右上角按钮同步禁用；响应到达后恢复正常渲染（失败亦复位）。E2E 实测点击瞬间：btnDisabled=true / "准备中…" / 思考气泡出现 / 右上按钮禁用，全部即时到位。

---

## 2026-08-30 · 修复：继续后新消息显示在结果面板上方（bug）

**反馈（原文照录）**："点了继续面试，但是新的面试官message不是显示在'面试结果'块的下面，而是显示在其上面。理想是显示在下面"。

**根因**：`_session_view` 的 detail 响应未暴露 `result_boundary` 字段，前端拿不到结果对应的消息边界，回退为"全部消息之后"，于是面板被画到整条消息流末尾，继续产生的新消息反而在其上方。

**修复**：`_session_view` detail 增加 `result_boundary`。E2E 回归：点击继续后顺序 [5条消息, 结果面板, 新面试官消息]，新消息在面板下方。

---

## 2026-08-30 · 续聊议程推进重构 + 面板位置恒定性严格验证（3 项）

**反馈（原文照录）**：① "你看一下'试一下哈'这个会话，是不是好像没问task，什么原因，你修好"；② "检查一下'面试结果/训练总结'在会话流里面的位置会不会保持恒定，如果不会的话要修。要严格检查"；（前序）"AI面试官还是一直针对同一个点追问"。

**根因与修复：**
1. **"试一下哈"没问 task**：旧续聊逻辑完全跳过议程推进，且 `answer()` 在续聊中继续累加 `cv_asked`、模型误输出 `[[CV_DONE]]` 标记时状态机会在续聊中翻转 phase 却不布置任务，造成 CV 永远问下去、task 永不出现。重构：续聊与正常面试走**同一套议程推进**——提前结束（task 未开始/未完成）的面试续聊时会继续走完议程，task 自然出现；任务全部做完后续聊则钳回最后一个任务自由追问（由 since_result 满 6 轮触发新结果）；自由续聊阶段用"考官主导、每轮换点"的专用措辞。
2. **追问单点**：总控 prompt 再加强（每问前回顾"是否问过"、优先未覆盖角度轮换），续聊自由段专用换题措辞。
3. **面板位置恒定性**：`result_boundary`/`message_count` 锚点在评分时固化，新消息只追加在后，面板位置天然恒定；发现的唯一漂移风险是**旧会话无 `result_boundary` 字段**（前端回退为"全部消息之后"会随续聊漂移）——已在双引擎 `continue_session` 回填。服务端渲染会话页（active 重进）此前只渲染消息丢面板——已改为 boot 时携带结果数据并重建消息流。

**严格验证**（无头 Chrome 对 stub 服务器）：scored 页 [5msg, PANEL, continue-bar] → 点击继续（即时反馈：禁用+"准备中…"+思考气泡+右上禁用）→ [5msg, PANEL, 新msg] → **退出重进（服务端渲染页）前后顺序完全一致** → 再答两轮后面板锚点位置不变（anchor_stable）→ 训练端同链路。冒烟 40 项全过。
